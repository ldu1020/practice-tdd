import React from "react";
import { Box } from "@chakra-ui/layout";

interface TodoProps {}

const Todo = ({}: TodoProps) => {
  return <Basis as="ul">hello world</Basis>;
};

export default Todo;

const Basis = Box;
